name: Feature 3 Deployment

on:
  push:
    branches:
      - feature-3

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Deploy
        run: |
          # Add commands to deploy the web application with feature 3
